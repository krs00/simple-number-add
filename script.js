function countUpBy(countBy, countTo){
    let Array = []

    for (let index = 0; index <= countTo; index += countBy) {
        
        console.log(index)
        Array.push(index)
        console.log(Array)

    }
};